// Fuse box properties
is_interacted = false; // Track if the fuse box has been interacted with
camera_object = obj_security_camera; // Replace with your camera object
popup_id = instance_create_layer(x-7, y-23, "Effects", obj_E_key);

popup_id.visible = false;
